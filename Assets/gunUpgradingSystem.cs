using UnityEngine;
using System.Collections;

public class gunUpgradingSystem: MonoBehaviour {

    int speedUpgradingPosition;
    int fireRateUpgradingPosition;
    int nextFireUpgradingPosition;

    //added variables from Montrel
    int xScaleUpgradingPosition;
    int yScaleUpgradingPosition;
    int zScaleUpgradingPosition;
    int destroyAfterUpgradingPosition;

    // Use this for initialization
    void Start () {
     //   speedUpgradingPosition = centralHub.addToList();
      //  fireRateUpgradingPosition = centralHub.addToList();
     //   nextFireUpgradingPosition = centralHub.addToList();

        //added variables from Montrel
      //  xScaleUpgradingPosition = centralHub.addToList();
     //   yScaleUpgradingPosition = centralHub.addToList();
     //   zScaleUpgradingPosition = centralHub.addToList();
     //   destroyAfterUpgradingPosition = centralHub.addToList();


    }
	
	// Update is called once per frame
	void Update () {
	
	}

}
